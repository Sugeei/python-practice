# coding=utf8
import logging
from logging import config

import yaml
import os
from common.config import logger

#
# with open(os.path.join(this_dir, "logging.yaml")) as f:
#     ycfg = yaml.load(f)
#     ycfg.setdefault('version', 1)
#     config.dictConfig(ycfg)
#
logger = logger
#
# logger.info("%s" % main_module)
